export type Professor = {
  matricula: string;
  nome: string;
  dataNascimento: string;
  especialidade: string;
  email: string;
};

export type Aluno = {
  matricula: string;
  nome: string;
  dataNascimento: string;
  email: string;
};

export type Course = {
  codigo: string;
  nome: string;
  descricao: string;
};
