export type Professor = {
  matricula: string;
  nome: string;
  nascimento: string;
  especialidade: string;
  email: string;
};

export type Aluno = {
  matricula: string;
  nome: string;
  nascimento: string;
  email: string;
};
